# ============================================================================
# AI-Agents .gitignore
# Owner: Cary Hebert
# Philosophy: GitHub is Source of Truth for CODE only.
#             Credentials, OS noise, and stale backups never touch this repo.
# ============================================================================


# ====================
# CREDENTIALS & SECRETS  ← HIGHEST PRIORITY — never negotiate on these
# ====================

# Clasp auth tokens (contain OAuth credentials for your Google accounts)
.clasprc.json
.clasprc.json.bak

# Any config file that is a backup/draft/old version
# Justification: Config.gs is already in the repo. A "config_old.gs" or
# "Config_backup.gs" sitting alongside it is almost certainly a version
# that still has hardcoded IDs or stale keys in it.
*_old.gs
*_backup.gs
*_old.json
*_backup.json
*_draft.gs
*_temp.gs

# Environment files (if ever added)
.env
.env.local
.env.*
*.env


# ====================
# macOS GHOST FILES  ← These bypass generic purge rules silently
# ====================

# .DS_Store files — generated by Finder every time you open a folder.
# The ** prefix ensures this catches them in ALL subdirectories, not just root.
# Note: If .DS_Store was already committed, see the README cleanup note below.
.DS_Store
**/.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.TemporaryItems
.Trashes
.fseventsd


# ====================
# IDE & EDITOR
# ====================

.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~


# ====================
# DEPENDENCIES & BUILD
# ====================

node_modules/
dist/
build/
.cache/
.parcel-cache/
__pycache__/
*.pyc
*.pyo


# ====================
# LOGS & TEMP
# ====================

*.log
logs/
*.tmp
*.temp


# ====================
# BINARY & OFFICE FILES  ← These belong in Google Drive, not Git
# ====================

*.xlsx
*.xls
*.docx
*.doc
*.pptx
*.ppt
*.pdf


# ====================
# PROJECT-SPECIFIC
# ====================

# Backup files (generic catch-all)
*.bak
*.backup
*.old
*.orig

# Clasp local project file backups
.clasp.json.bak

# Database exports (may contain PII or personal data)
db/*.csv
db/*.xlsx

# RAG engine — generated indexes and raw source files never belong in Git
standards_chroma/
standards_raw/*.pdf
standards_raw/*.csv

# Archive and deprecated folders — Git history IS the archive
# Keep the folder placeholder but ignore all contents
archive/**/*
!archive/.gitkeep
deprecated/**/*
!deprecated/.gitkeep


# ====================
# ALWAYS ALLOW  ← These must never be accidentally ignored
# ====================

!.gitignore
!.github/
!dev-project/.clasp.json
!prod-project/.clasp.json


# ====================
# NOTES FOR FUTURE CARY
# ====================
#
# If .DS_Store appears in "git status" as tracked, run this once to evict it:
#   git rm --cached .DS_Store
#   git rm --cached **/.DS_Store
#   git commit -m "chore: remove tracked .DS_Store files"
#
# If a Config_old.gs or Config_backup.gs was accidentally committed, run:
#   git rm --cached dev-project/Config_old.gs
#   git commit -m "security: remove stale config file from tracking"
#
# Stale branch cleanup (run monthly or after any abandoned experiment):
#   git branch -r | grep -v 'main' | grep -v HEAD
#   git push origin --delete <branch-name>
#
# Git reflog cleanup (run before major pushes to keep .git folder lean):
#   git gc --prune=now --aggressive
# ============================================================================


# ====================
# iCLOUD PLACEHOLDERS  ← Prevent iCloud offload stubs from entering Git
# ====================

# When macOS offloads a file to iCloud, it replaces it with a lightweight
# stub ending in .icloud. These are NOT the real file and must never be
# committed. The .nosync suffix opts folders out of iCloud sync entirely.
*.icloud
.icloud
**/*.icloud
*.nosync
